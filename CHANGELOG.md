# 更新日志 (Changelog)

本文件记录 spiral-emergence 项目 v15 版本的所有重要变更。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)。

## [v15.0] — 2026-09-19

> **范围说明：** v15 是一个以方法学为核心的版本。七层框架不声称已确立（审计成熟度：A×1, B×3, C×3, D×0）。物理基底为教科书级伊辛临界性。新颖性位于方法层（负对照账本、完整性守护、计分/诊断分离）。

---
### 新增

- **负对照账本**（`spiral_metric_v15._NEG_CTRL_TABLE`）：12 个对照覆盖 7 层中的 6 层（缺口：L1）。四类：排斥(5)、双路径(5)、零模型(1)、上界(1)。明确排除正性/有限性/非空性检查。
- **完整性守护**：将账本与运行时注册表交叉核对；检测幽灵引用和缺失层。修复前捕获 3 个幽灵引用 → 修复后 0 个。
- **计分/诊断分离**：54 个计分守卫 + 20 个诊断项（17 个按设计失败，排除出分母）。`expect_pass` 标志在注册时显式设置。
- **外部 CFT 锚点**：逐 L 的 1/L² 系数与无参数公式 `e∞ − πcv/(6L²)` 比较；残差反转幂 p=4.00（L=8→18）。
- **独立第二估计量**：低能谱比值 (E₂−E₀)/(E₁−E₀) 给出 c ∈ [0.49894, 0.50034]（最大偏差 0.212%），独立于 Rényi 族（偏差 7.97%）。
- **MERA 体几何不变性**：已记录 MERA 图不依赖状态、种子和 χ（在种子 0/1/7 和 χ∈{2,4,8} 下逐位一致验证）。
- **W2 参考侧重建**：重写 `spiral_v15_prepare.py` 用于离线描述子稳定性检查。
- **W4 L7 Jacobian 谱半径**：新增 `l7_rho_jac_B` 上界对照。

### 变更

- **W0 骨架**：版本号 v14→v15；缓存目录重命名；相对于 v14 基线零逻辑改动。
- **W1 selfref_coupled**：升级为完整 N 相图扫描，含 5-seed 平均和 RANK_TOL 敏感性分析。
- **W3a 面积律**：新增 h/J 扫描（0.5, 1.0, 1.5）以区分对数律（临界）与面积律（偏离临界）。
- **W3b 谱学 c**：新增独立估计量，与 Calabrese-Cardy 交叉检验。
- **W5 spiral_loop**：通过墙钟探测设定经验 `max_L` 上限；启用 c(L) 和 ξ/L(L) 外推。
- **守卫记账**：日志行格式 `guards 54/54 = 100.00% (plus 20 diagnostics excluded, of which 17 did not pass)`。
- **预印本**：`spiral_v15_preprint.tex`（英文）+ `spiral_v15_preprint_zh.tex`（中文翻译，ctexart 文档类）。

### 修复

- **完整性判据反转缺陷**：三个账本条目写入自报告名称，仅在提前返回分支注册 → 对照缺失时检查通过，正常时反而失败。已修复；幽灵引用 3→0。
- **max_L 归因**：源码注释错误地将 L≤16 上限归因于 ED 限制；修正为 MERA 库约束（要求 L = 2 的幂）。
- **自相矛盾的报告约定**：(i) 散点统计误标为"种间 spread"；(ii) 两条路径声称"相互印证"实则为同一测量。两者均在源码中修正。
- **F6d 诊断**：重命名并更改条件字符串；确认总计不变（诊断不进入分母）。

### 新增引用

| arXiv | 引用角色 |
|-------|----------|
| 0707.1454 | MERA 算法基础（Evenbly & Vidal） |
| 1209.3304 | 纠缠重整化与全息上下文（Swingle） |
| hep-th/0405152 | 锚点 CFT 有限尺寸公式来源（Calabrese & Cardy） |
| 1412.0732 | 张量网络重整化基线（Evenbly & Vidal） |
| 1501.01924 | 计算可重复性框架（Hinsen） |

### 可重复性

- **单条命令**：`python spiral_model_v15.py`（单台笔记本约 20 分钟）
- **产物**：`spiral_v15_metrics.png`（20 面板指标报告）、`result/_v15_data.json`、标准输出账本
- **预期输出**：`guards 54/54 = 100.00%`、`phantom references: none`、`missing layers: none`、6/7 层覆盖（缺口 `['L1']`）
- **代码**：自包含于 `v15/` 目录；伊辛层不读取外部数据；Gray-Scott 展品使用已发表数据集。