# 更新日志 (Changelog)

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)。

## [v15.0] — 2026-09-19

> **范围说明：** v15 是一个**方法学版本**。七层框架**不声称已确立**，成熟度为 1A + 3B + 3C + 0D（相对 v14 的唯一变化是 **L7 由 D 升 C**）。物理基底为教科书级伊辛临界性，新颖性位于方法层：负对照账本、完整性守护、计分/诊断分离。

---

### 新增

- **逐层负对照账本**（`spiral_metric_v15._NEG_CTRL_TABLE`）：12 条对照覆盖 7 层中的 **6 层**（缺口：`L1`）。类型分布 `推离 5 / 二路径 5 / 零模型 1 / 上界 1`。判据比"有个检查"严——必须在结论不成立时能失败，故正性/有限性检查被明确排除在账本外（它们仍以普通守卫留在通过率里，只是不在这里充数）。
- **台账完整性守护**：把账本与运行时守卫注册表交叉核对，检测幽灵引用与缺失层。
- **计分 / 诊断分离**：54 个计分守卫 + 20 个诊断项，其中 17 个**按设计就该失败**（如 `V6`：派生 N 之后变体 A 收敛到 x\*=0，权重失去方向），排除出分母。
- **外部 CFT 锚点**（`_v15_anchor_external.py`）：`E0(L)/L = e∞ − πcv/(6L²)`，取 c=1/2、v=2、e∞=−4/π，**无自由参数**。L=8…18 相对残差 2.88e-5 → **1.81e-6**(L=16) → 1.13e-6(L=18)，单调退去；残差反解幂次 **p ≈ 4.00**。
- **第二把尺子**：低能能谱比值 `(E₂−E₀)/(E₁−E₀)` 7.9231(L=8) → 7.9807(L=16)，单调趋 **8**；c 落在 **[0.49894, 0.50034]**，最大偏差 **0.212%**（对照 Rényi 族 7.97%）。能谱族**独立于纠缠谱族**。
- **W4 L7 Jacobian 谱半径**：B 支 `defined=True` 且谱半径 0.9412 ≠ 1（非退化）。

### 变更

- **W0 骨架**：版本号 v14→v15、缓存目录重命名，**相对 v14 基线零逻辑改动**；判据与阈值 `EPS_T=0.05` / `RC_T=1e-2` 一字未动（本层是"没有回归"的证据）。
- **W1 selfref_coupled**：升级为完整 N 相图扫描，含 5-seed 平均。
- **W3a 面积律**：新增 h/J 扫描（0.5 / 1.0 / 1.5），区分对数律（临界）与面积律（偏离临界）。
- **W5 spiral_loop**：以墙钟探测设定经验 `max_L` 上限；启用 c(L) 与 ξ/L(L) 外推。
- **A 段参考侧**：换成与模型侧**逐字同参数、同单位**的受控重算（取代此前不可归因的跨来源比较）。
- **预印本**：`v15/spiral_v15_preprint.tex`（英文、向 arXiv 格式、**零宇宙学内容**）+ `v15/spiral_v15.bib`。

### 修复

- **完整性判据反转缺陷**：3 个账本条目写入自报告名称、且只在提前返回分支注册 → 对照缺失时检查通过、正常时反而失败。修复后幽灵引用 **3 → 0**。
- **`max_L` 归因错误**：源码注释把 L≤16 上限归因于精确对角化边界；实为 **MERA 要求 L 为 2 的幂**（稀疏 ED 本身没有 16 这个上限，L=18 整行读数正常）。已改正。
- **两处自相矛盾的报告口径**：(i) 散点统计误标为"种间 spread"；(ii) 两条路径声称"相互印证"，实则为同一次测量。
- **G5 曲率解析锚点**：此前只打印 `max|实测−解析|` 而无任何判定（"有数字无判定"的洞），现补成 bool。
- **`T3-4b` Reading 谱峰**：带内 argmax 周期不可用时三条序列根本没进入自相关检验（0/3 份），属**通路问题**而非物理结论不成立；根因是两个常数按 BZ 空间序列标定、直接套到强度序列上不成立。

### 目录结构

- 源码与数据按版本分目录：v14 全量移入 `v14/`，v15 全量移入 `v15/`（纯重命名，零逻辑改动）。各版本自包含：`spiral_model_v*.py` / `spiral_metric_v*.py` 在各自目录内运行。

### 引用

| arXiv | 引用角色 |
|-------|----------|
| 0707.1454 | MERA 算法基础（Evenbly & Vidal） |
| 1209.3304 | 纠缠重整化与全息上下文（Swingle） |
| hep-th/0405152 | 锚点 CFT 有限尺寸公式来源（Calabrese & Cardy） |
| 1412.0732 | 张量网络重整化基线（Evenbly & Vidal） |
| 1501.01924 | 计算可重复性框架（Hinsen） |

### 可重复性

- **单条命令**：`cd v15 && python spiral_model_v15.py`（单台笔记本，**实测 1245 s**；预算 ≤4000 s；v14 基线 1018 s）
- **产物**：`v15/result/spiral_v15_metrics.png`（20 面板）、`v15/result/_v15_data.json`、标准输出账本
- **预期输出**：`通过率 = 54/54 = 100.0%`（另有诊断项 20 项，其中未通过 17 项）、幽灵引用 `无`、缺失层 `无`、覆盖 **6/7 层（缺口 `['L1']`）**、指标 **18/18**（v14：13/13）
- **代码**：自包含于 `v15/`；伊辛层不读取外部数据；Gray-Scott 展品使用已发表数据集。

### 已知边界（照实登记）

- **`L1` 仍无负对照**。已查明原因而非搁置：L1 到下游**没有因果通路**，"推离初态"这条控制形式造不出来，缺口如实留着。
- **0.212% 不许写成"能谱族与 Rényi 族互相印证"**。"ratio→8" 与 "c→0.5" 是**同一个测量**（0.212% 就是 ratio 偏离 8 的量）；正确口径是"能谱族**独立于**纠缠谱族"。且该读数**不随 L 单调收敛**（L=16 反而最差，残差为 O(1/L) 有限尺寸修正），只能说"相容到 0.21%"，**不能**说"外推到 0.5"。
- **L4→L5 桥梁仍空**；第三层（结构对结构）明确**降级为方法学展示**，不设物理达标线。
- **预印本 `.tex` 未编译验证**——本机无 `pdflatex`。
