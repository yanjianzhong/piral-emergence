# v14 说明 · 七项检验升级（负对照 / 纠错 / 去混杂）

本次运行 `python spiral_model_v14.py`，总用时 **1018 s**（`_v14_run.log`），产物
`result/spiral_v14.png` / `result/spiral_v14_metrics.png` / `result/_v14_data.json` /
`result/_v14_hj_scan.png`。

v14 不加新物理阶段，不动因果链派生叙事（L6 的 N=36 由 xi/L=1.2071 派生，不可改），
全部工作在 L≤16、chi≤4（chi=8 仅作对照）内完成。七项任务的落点是**让已有结论可证伪**：
加负对照、加独立解析路径、把错误判据换成对的，并如实报出量到的负面事实。

体检报告：

| 层 | v13 | v14 | 说明 |
|---|---|---|---|
| 第1层 内部自洽性 | 达标 5/5 | 达标 **5/5** | 判据与阈值未放宽 |
| 第2层 微观物理对标 | 达标 4/4 | 达标 **8/8** | 新增 2.5~2.8 四项独立检验 |
| 第3层 宏观结构对标 | 达标 0/0（诊断 5） | 达标 0/0（诊断 5） | 仍为方法学展示，不设物理达标线 |
| 守卫 | 22/22 | **39/40 = 97.5%** | 分母扩大；1 条如实不通过，见 §7 |

合计 达标 **13/13**，另有诊断项 9 条（其中 8 条**设计上就该不通过** —— 它们是留档的
负面结果，故意不计入分母）。

---

## Update list（本次升级逐点一句话）

### 一、新增检验（把"结论"变成"可证伪的结论"）

1. **F7 边界极值审计** —— 给 `_char_scale` 加输入校验包装 `_char_scale_checked`：空场抛可读
   `ValueError`，全零/全一/常数场标 `valid=False` 并回退 `lam=box`，周期条纹标 `valid=True`；
   取峰规则（argmax）一字未动。
2. **F5 一致性检查束** —— 给 L2/L3/L5/L6/L7 各配一条独立解析路径做双路对照（eigsh ↔
   Jordan-Wigner 闭合式、幂迭代残差 ↔ 谱隙理论、`4-deg(u)-deg(v)` ↔ 环 C_n 解析 0、
   `Σ Lap(u) ≡ 0`、逐步离散平衡恒等式、熵比/等权度上界），L2/L3/L5/L6a/L6b 全部 < 1e-12。
3. **F5 判据纠错（Task 2）** —— 任务书原定的 L6「u+v 质量守恒 < 1e-6」被实测**证伪**
   （首步漂移 6.25e-5，是声明容差的 62 倍；源汇项 F(1-u) 与 (F+k)v 本就不相消），L7
   「x\*=0 时熵比恰为 1」在闭环分支上恒取等号、无分辨力；两者换成上列正确替代不变量，
   原判据错在哪写进日志。
4. **F2a h/J 负对照扫描** —— 在 h/J ∈ {0.5, 0.8, 0.9, 1.0, 1.1, 1.2, 1.5} 外加临界区
   {0.95, 0.98, 1.02, 1.05} 加密，逐点精确对角化 + Calabrese-Cardy 拟合，量出 c_fit 峰位
   = 1.0（0.5072）、S 峰位 0.95、四条区分力；任务书原判据「gap 在 h/J=1 取极小」实测
   **不成立**（gap 对 h/J 单调 0.9967→0.0398，照原判据会伪失败），已改用 c_fit 峰位。
5. **F1 跨边界条件中心荷交叉检验** —— 新增 θ=π Z₂ twist 边界条件与 PBC 并排量 c
   （每边 3 种子 × 1500 步，检查点按 overlap 选），相对差 3.36% ≤ 5%；明确标注是**同族**
   检验（同一个 Calabrese-Cardy ansatz），**独立族估计量未建立** —— Rényi 指数扫描
   α ∈ {0.5…8.0} 实测均值 c=0.5399、对 0.5 偏差 7.97%，验证不通过，降为诊断项。
6. **F3 去混杂 + 重播种** —— 参考侧从归档 bool 场换成 12 个自建 Gray-Scott 种子（两侧同用
   `v > 中位数` 规则），D_struct 由 0.9331 降到 **0.4221**、种子间散布 0.4544→0.5649、
   判别力 d′ 由 2.05 降到 **0.75**，旧值作为已发布基线并列报出，**不声称旧值被"修正"**。
7. **F6 涌现几何对照鲁棒性** —— 把原来 1 个对照实例扩成 3 族（gnm / ws / tree）× 3 实例
   = 9 个，报 Forman 与 Ollivier 两个测度的百分位（都是第 33 百分位）；升级措辞
   「MERA 比全部对照更负」实测为 **False**，结论降为"MERA 体几何具有负曲率特征，但在同类
   规模的零模型里不是统计异常"。
8. **F4 B 段断言检验** —— Reading 三条序列加谱域高通 + 带内**内部**局部极大 + 突出度判据
   （阈值按零假设 300 次红噪声抽样标定），并对"裁预热"做敏感性扫描（9 种 序列×裁剪 组合
   全部未检出），让"未检出"从"能力不够"变成"有能力的未检出"；漂移占比的低估被量化出来，
   但**不许它驱动裁剪**（改裁剪就是改已经报出去的口径）。

### 二、工程口径

9. **注释与日志口径统一** —— 两个源文件（`spiral_model_v14.py` / `spiral_metric_v14.py`）
   的注释统一到 v14，不再保留历年版本迭代叙述；运行日志删去解释版本迭代的冗余段落，
   只分阶段输出事实数据。
10. **报告结构** —— 指标图扩到 20 面板（第一层 4 格、第二层保留 2.3/2.4 两张独立断言的
    唯一可视化、A 段 5 行），守卫首次按"预期通过 / 诊断"分离计分。

---

## 1. 第一层：判据与阈值均未放宽

`EPS_T = 0.05`、`RC_T = 1e-2` 与已发布基线逐字相同。

| 量 | 值 |
|---|---|
| eps_c 五条轨迹全距 | **[0.79%, 3.86%]**，中位数 3.03% |
| R_conf 五条轨迹全距 | **[1.097e-03, 2.659e-03]**，中位数 1.764e-03 |
| 最坏轨迹（1.5 全程稳健性） | lr=0.03 seed=1，eps_c 3.8578%，R_conf 1.764e-03 |
| 1.6 预算漂移（诊断） | 4.919% (step 700) → 3.278% (step 1500)，**−33.4%** |
| 1.7 chi 瓶颈（诊断） | chi_gain 4→8 = **1.61×**；optim_gain 600→1500 步 = **3.78×** |

**结论**：chi=4 同预算下已达标（3.03% < 5%），已发布基线的 11.45% 落在本分布的右尾，
是"未收敛"而非"表示极限"。1.6 说明报出值仍是**上界**（还在下降），因此**不能**声称已
收敛到该 chi 的最优值。

---

## 2. 第二层：新增四项独立检验

| 项 | 定义 | 实测 | 参照 |
|---|---|---|---|
| 2.5 临界性负对照 | h/J 扫描中 c_fit 的峰位 | 峰位 **1.0**，c=0.5072 | 解析 h/J=1 |
| 2.6 跨边界条件中心荷（同族） | \|c_twist − c_PBC\| / c_PBC | **3.36%** ≤ 5% | 体中心荷不随边界条件变 |
| 2.7 一致性检查束 | 5 条双路偏差 | 全部 ≤ 1.8e-14 | 见下表 |
| 2.8 涌现几何对照鲁棒性 | MERA 在 9 个零模型实例中的百分位 | Forman / Ollivier 均 **33%** | 三类零模型 |

原 2.1~2.4（RMSE 0.0076 / KL 0.0014（JS 1.160e-04）/ η 0.2426（精确 0.25）/
Jordan k\* 相对误差 3.25%）定义与阈值一字未改，**正是"没有回归"的证据**。

### 2.7 五条双路对照的细节与边界

| 阶段 | 独立路径 | 实测偏差 |
|---|---|---|
| L2 | Jordan-Wigner 闭合式 `E0=−Σ√(J²+h²−2Jh·cos(π(2k+1)/L))` vs eigsh，5 个 (L,h) 点 | 1.78e-14 |
| L3 | 幂迭代末段步长比 vs 解析 \|λ2/λ1\|，5 个算例 | 8.64e-14 |
| L5 | 环 C_n（n≥4）的 Forman 均值 vs 解析 0 | 0.00e+00 |
| L6a | 周期域上 `Σ Lap(u) ≡ 0` | 5.09e-17 |
| L6b | 同一步的离散平衡恒等式（用更新前的场算） | 2.66e-16 |

- L2 这条是**真的独立路径**：它能测出 H 的位序、周期键、横场符号写错，而 eigsh 对 H
  本身对不对一句话都没有。
- L3 原判据写的是「与 Lanczos 100/200/500 步无关」，但 eigsh 没有步数参数；换成 maxiter
  后 30 以上逐位相同，**零分辨力**，故改成残差断言。
- L5 的 **n=3 已排除**：C₃ 就是 K₃，augmented 的 +3 项把它顶到 +3，不是 0。
- L6 的两条是**格式性质** —— 保证代码按写下的方程在算，**不保证那个方程描述的是生命**。

---

## 3. 第三层 A 段：口径变更（必读）

| 量 | 已发布基线 | v14 | 依据 |
|---|---|---|---|
| D_struct | 0.9331 | **0.4221** | 参考侧换代 |
| 种子间散布 | 0.4544 | **0.5649** | 12 个自建种子 |
| 比值 / d′ | 2.053 | **0.747** | 判别力只有 0.7 倍噪声底 |
| R_P（A2） | 1.277 | 1.7468 | 换成同方程形式的二维参考 |

**参考侧为什么换**（两条独立事实，均出自 A 段诊断项）：

1. 归档 bool 场与同一归档的浮点场**任何简单二值化都对不上**（逐格一致率 seed 0: 0.5688 /
   seed 5: 0.5025 / seed 10: 0.5041，≈ 随机猜测），其 100²/300 帧生成管线不在归档里。
2. 归档 `data_make.py` 的初始条件是硬编码的，`np.random.seed` 与 `random.seed` 设了却从未
   被使用 —— 三个"种子"实际同出一个 IC（三个浮点场统计量几乎相同）。

**后果**：已发布基线的 scatter 不是种子间散布，它测的是一条不可复现管线的抖动；其
lam_pk 中位数 1.4514 格恰是二维离散谱角点（1.4142 格），是那种场里 1 格台阶的伪像。

**边界**：换掉参考侧后 D_struct 降了 55%，**这不等于"模型变好了"** —— 它说明旧值里有
相当一部分距离来自坏参考场，而不是来自模型与参考的物理差异。新旧值并列报出，不声称
旧值被"修正"。

另有一条被本次实测**否掉一半**的说法：角点伪像不是 `_char_scale` 的固有 bug，它取决于
输入场 —— 换成自建种子后 lam_pk 中位数变成 28.75 格，离角点很远。

---

## 4. 第三层 B 段：结论仍是"未检出"，但性质变了

- 三条 Reading 序列**全部未检出周期**，这是**结论**而非缺失：凝胶存在相位不均匀的螺旋波，
  全局平均亮度会把反相的局部振荡抵消掉，只剩慢漂移（仅去线性趋势后 k<8 仍占 51%~78% 的功率）。
- 检出判据要求峰是"带内**内部**的局部极大"且突出度 ≥ 300 倍局部背景；该阈值按零假设标定
  （纯红噪声 300 次抽样突出度最大 113，而 T=25~200 帧的正弦信号最小 2309）。
- **必须说明的一处污染**：三条序列头部是相机预热暗帧（亮度 ~25.6 vs 稳态 84~115），是一段
  硬阶跃，会给整条谱灌高频功率，从而把漂移占比**压小** —— 报出的漂移数是**被低估的下界**。
  按"掐掉头部 N 帧"重算：10%→29%→90% / 15%→73%→73% / 16%→88%→87%。
  **但结论不受影响**：全部（序列 × 裁剪）组合都未检出。
- BZ 三条记录周期中位数 [15.0, 24.5, 33.6] s，最大/最小 2.24；不设达标线 —— figS1C 的
  CV=0.899 是**那条记录的真实性质**（不规则爆发式波形），把记录的性质当成流程的达标项
  是判据错配。

---

## 5. 判据纠错（Task 2）：被证伪的两条原判据

任务书原文要求「L6 检验质量守恒（u+v 的网格均值每步变化 < 1e-6）」「L7 检验熵比和等权度在
x\*=0 时精确为 1.0」。实测两条**都不成立**，且是**判据本身的错**，不是实现没做到：

- **L6 的「u+v 守恒」是伪不变量**。Gray-Scott 的源汇项 `F(1−u)` 与 `(F+k)v` 不相消，
  所以 `d⟨u+v⟩/dt ≠ 0`。实测首步漂移 **−6.250e-05**，是声明容差 1e-6 的 **62 倍** ——
  任何正确实现都过不了这条。换成两条真不变量：周期域上 `Σ Lap(u) ≡ 0`（实测 5.09e-17）、
  以及用更新前场算的逐步离散平衡恒等式（实测 2.66e-16）。
- **L7 的两条上界在闭环分支上恒取等号**。`x* = 0` 那一支使熵比与等权度都恒为 1.0000，
  判据无分辨力（本项 `defined=False`）。改为把闭环分支的真实读数量化并报上界是否成立，
  **不把恒等式当成检验通过**。

两条错误判据与被证伪的方式都写进了日志，未静默替换。

---

## 6. F6 与 F7 的措辞边界

- **F6 不升级措辞**：MERA 的 Forman 均值 −2.242 位于 9 个对照实例的第 33 百分位，
  Ollivier 亦然；「MERA 比全部对照更负」为 **False**。故结论是"具有负曲率特征，但不是统计
  异常"。**边界**：这是零模型对照，不是同一物理系统的不同实现 —— 百分位不特别**不**支持
  "MERA 就是全息对偶"，落在区间外也**不**构成对涌现几何整体主张的否证。三角形数与平均度
  必须与曲率一起读：本工作用无三角形式 `F = 4 − deg(u) − deg(v)`，它只在图接近树时才与
  完整 Forman 等价，而 MERA 图三角数 = 0。
- **F7 只加校验，不动取峰规则**：原调用方的守卫 `isfinite(lam) and lam > 0` 接不住平坦场
  —— 平坦场的 lam 恰好有限且为正（= box），**那不是守卫失效，是本就不为这件事写的**。
  实测：常数场 lam=30.8831（已标无效），周期条纹（周期 9 格 / 盒子 36 格）lam=8.8326 格。
  **诚实边界**：`valid=False` 只说"这次输入不满足定波长的前提"，不说 lam 一定是错的；
  `valid=True` 也不说 lam 有独立佐证 —— 那一条由 A 段诊断项回答，而它**未通过**（见 §7）。

---

## 7. 未通过项与未决事项（照实登记）

1. **守卫 39/40，唯一一条真失败：`T3-4b Reading 谱峰经自相关交叉检验`。** 原因是没有可用
   序列（带内 argmax 周期不可用，0/3 份做了自相关滞后检验）。这条守的是"Reading 侧也有
   第二条独立路径"，实测**这条路径不存在**，如实记为未通过，不做降级处理。
2. **`lam_pk` 缺少独立佐证（诊断项，未通过）**。与"同一估计量作用在未二值化的 v 场"相比
   中位偏差 12.8%（最大 231%），与**真正独立**的自相关尺度 `4*xi_ac` 相比中位偏差 46.6%
   （最大 98%），且是**系统性**的（自相关尺度恒比 lam_pk 大：中位数 58.0 格 vs 28.7 格）。
   `_char_scale` 取 Δ²(k) 在**离散壳**上的 argmax，所以本段 12 个种子只给出 **6 个不同的**
   lam_pk。**为什么不改**：修它需要一个有物理依据的取峰规则，而试过的替换方案（在 λ ≥ k
   的壳里取 P(k) 最大者）在合成条纹上验证通过（7.692 / 16.667 vs 真值 8 / 16），但在真实场
   上退化到盒子尺度的低频团块（三个参考种子都给 46.841），稳定性不达标 —— 拿一个未验证的
   估计量换掉一个已知有缺陷的估计量，风险大于收益。故按用户决定照实报缺口。
   **边界**：本段的 lam_pk 与 n_lam 只能按"某条离散壳上的一个读数"读，既不保证是斑图波长，
   也没有独立佐证；涉及"盒子内波长数"的结论一律降级为量级陈述。
3. **`phi` 在本口径下不携带任何结构信息**（诊断项，未通过）。两侧同用 `a > median(a)`，
   所以 phi 恒在 0.5 附近，它只是一条恒等式。**旧口径的实际后果更糟**：模型侧走中位数
   （phi ≡ 0.5），参考侧走归档 bool 场（phi 0.6378/0.9181/0.9125），于是一个**恒量**去比
   一个**变量** —— 逐项距离里 phi 一项就贡献 0.3964，而那条 D_struct 是 0.9331，即**四成
   以上的"距离"来自这条恒等式**。换两侧同规则后 phi 贡献恰好 0，恒等式因此无害。
   **边界**：无害 ≠ 好描述子。保留它是为了不静默改变描述子集合的维度。
4. **独立族中心荷估计量未建立**（诊断项，未通过）。Rényi 指数扫描 α ∈ {0.5, 0.75, 1.5, 2.0,
   3.0, 5.0, 8.0} 实测均值 c = 0.53986、std = 0.02252，对 0.5 偏差 7.97%，验证不通过。
   这是受 L ≤ 16 天花板限制的结果，已如实声明；**未伪造第二把尺子**。
5. **`spiral_metric.py`（v12 冻结快照）未被改动**，因此日志里仍有三处旧版本字样：
   头部 `体检报告 · 三层量化指标 (v12)`，以及 1.4 / 2.1 两条 note 里的版本叙述。该文件被
   v11/v12 共用，改动会改变 v12 的标准输出与可复现性，故按既有约定保留 —— 这也与 v13 的
   做法一致（`_v13_run.log` 同样打印 `(v12)`）。如需一并清掉，请明确指示。
6. **`spiral_v13_说明.md` 未改动，但其 A 段有两条说法已被本次实测推翻**：
   「scatter 是参考侧种子间散布」与「两条独立路线互相印证」。依据是 A 段两个未通过的诊断项；
   运行日志的汇总段照实列出了这件事。是否回改那份已发布文档，留待决定。
7. **Task 1 验收标准里的函数名与实现不同**：任务书写的是"新增输入校验函数
   `validate_input_bounds()` 被调用"，实际实现为 `_char_scale_checked`（只覆盖 `_char_scale`
   这一条链路，因为按用户确认的范围只做输入校验、不动取峰规则）。功能等价，命名不同，
   在此登记以免验收时对不上号。

---

## 8. 依赖与运行

- 依赖未变：`numpy` / `scipy` / `matplotlib` / `networkx` / `quimb`。
- 运行：`python spiral_model_v14.py`（约 1020 s）。
- 离线缓存：`data/_v14_cache/gs_seeds.npz`（12 个自建 GS 种子，由 `spiral_v14_prepare.py` 生成）；
  归档侧仍用 `data/_v13_cache/`。
- 产物：`result/spiral_v14.png`、`result/spiral_v14_metrics.png`（20 面板）、
  `result/_v14_data.json`、`result/_v14_hj_scan.png`、`_v14_run.log`。
